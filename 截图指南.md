# OneDay 报告截图指南

> 用途：为《实践课作业》第 4 节（测试）的每个「插入截图」占位提供**拍什么、怎么拍**的逐步说明。
> 共 14 张图，按编号拍完即可一一对号入座。

---

## 〇、开拍前的准备

### 1. 启动应用

按学校机器流程启动（本机调试同理）：

```
1. 启动 PostgreSQL（startPG.bat，等日志出现 "listening on ... port 5432"）
2. 启动 Bun 控制台（StartBun.bat）
3. cd 到 server 目录
4. bun run migrate   （首次或 schema 有变更时）
5. bun run dev
6. 浏览器打开 http://localhost:3000
```

登录账号：学号 `S20260530`，密码 `123456`。

### 2. 把浏览器切成"手机视图"（强烈建议）

本应用是移动端布局（底部有"首页 / 心愿库 / 图表 / 设置"导航栏），用手机视图截图最整洁、最像真实 App：

1. 浏览器按 `F12` 打开开发者工具；
2. 点左上角的「设备模拟」图标（手机/平板图标，快捷键 `Ctrl+Shift+M` / Mac `Cmd+Shift+M`）；
3. 顶部设备选 **iPhone 12 Pro（390 × 844）** 或 iPhone 14；
4. 把缩放设为 100%，关掉 DevTools 面板只留模拟画面再截图。

### 3. 截图快捷键

| 系统 | 框选截图 |
|---|---|
| Windows | `Win + Shift + S`，鼠标框选内容区 |
| macOS | `Cmd + Shift + 4`，鼠标框选内容区 |

要点：**只截浏览器内容区**，不要把浏览器地址栏、书签栏、操作系统任务栏框进去。

### 4. 文件统一放一个文件夹

在报告同目录新建 `images/` 文件夹，所有截图按本指南的"文件名"保存进去，方便后面插入 Word 或 md。

---

## 一、4.2 接口/正常功能 截图（图1~图4）

### 图1　登录页

- **目的**：展示登录界面与表单。
- **操作**：打开 `http://localhost:3000`，未登录会停在登录页（`/login`）。
- **拍什么**：完整登录卡片——学号输入框、密码输入框、"登录"按钮、底部"去注册"链接。**不要**填入密码（或填星号即可）。
- **文件名**：`fig-4-2-login.png`

### 图2　登录后首页（仪表盘 + 物品列表）

- **目的**：展示主界面与统计卡片。
- **操作**：用 `S20260530 / 123456` 登录，进入首页。
- **拍什么**：顶部的**总资产、日均成本**等仪表盘卡片 + 下方至少 3~4 件物品的列表卡片（带图标、名称、价格）。让卡片和列表都入镜。
- **文件名**：`fig-4-2-home.png`

### 图3　心愿物品（移入资产**前**）

- **目的**：证明心愿物品不计入总资产。
- **操作**：
  1. 先记住当前首页"总资产"数字（可一并入镜）；
  2. 点 + 新建一件物品，顶部切到"**心愿**"，名称填"心愿-相机"，价格 `2000`，保存；
  3. 切到底部"心愿库"页。
- **拍什么**：心愿库里这张"心愿-相机 ¥2000"卡片，卡片上有"**移入资产**"按钮；顶部"心愿合计"显示 2000。**此时不要点移入。**
- **文件名**：`fig-4-2-wish-before.png`

### 图4　移入资产**后**

- **目的**：证明移入后总资产 +2000 且生成购入事件。
- **操作**：点上一步那张卡片的"移入资产"，确认；然后回首页。
- **拍什么**：① 心愿库里该物品已消失（或②首页总资产比图3多了 2000）。建议截首页，让"总资产"数字入镜，与图3 形成对比。
- **文件名**：`fig-4-2-wish-after.png`

---

## 二、4.3 异常/校验 截图（图5~图6）

### 图5　注册表单校验报错

- **目的**：展示前端 `rules` 即时校验。
- **操作**：进注册页，**故意填错**：手机号填 `123`、邮箱填 `abc`、密码填 `12`（太短），其余留空，点一下别的输入框让校验触发。
- **拍什么**：多个输入框下方的**红色错误提示文字**（如"手机号必须为 11 位""邮箱格式不正确""密码 6~20 位"）。让 2~3 条红字同时入镜最有说服力。
- **文件名**：`fig-4-3-register-error.png`

### 图6　错误密码登录提示

- **目的**：展示后端返回 401 的前端提示。
- **操作**：登录页输入正确学号 `S20260530`，密码故意输错（如 `000000`），点登录。
- **拍什么**：页面弹出的"**学号或密码错误**"提示（snackbar / 红字）。
- **文件名**：`fig-4-3-login-401.png`

---

## 三、4.4 界面功能 截图（图7~图14）

### 图7~图9　UI-01 主题色切换 + 持久化（三连图）

> 这组是重点，要拍**前 / 后 / 刷新**三张，证明换色不仅生效、刷新还不回弹。

- **图7（换色前）**
  - **操作**：进"设置"页，进入前先看当前主题色（比如默认粉色）。
  - **拍什么**：当前配色下的设置页或首页（让主色调入镜，作为"换色前"基准）。
  - **文件名**：`fig-4-4-theme-before.png`
- **图8（换色后）**
  - **操作**：设置页点"主题色"→ 打开主题色画廊 → 选一个**明显不同**的颜色（如蓝/绿）。
  - **拍什么**：选中后界面**立即变成新色**（按钮、卡片高亮都变色）。
  - **文件名**：`fig-4-4-theme-after.png`
- **图9（刷新后）**
  - **操作**：按 `F5` 刷新整个页面。
  - **拍什么**：刷新完成后**仍是新色**（没有变回旧色）——这证明颜色存进了 `localStorage`。
  - **文件名**：`fig-4-4-theme-reload.png`

### 图10　UI-02 图标选择器三 Tab（停在 3D）

- **目的**：展示"相册 / Emoji / 3D"三选一图标选择器，尤其是下载的 3D 矢量图标。
- **操作**：点 + 新建物品 → 点中间那个大图标区域 → 打开图标选择器 → 切到"**3D**"Tab。
- **拍什么**：弹窗顶部的三个 Tab（相册 / Emoji / 3D）+ 3D Tab 下的图标网格（哔哩哔哩、耳机、信用卡、工具等彩色 3D 图标）。
- **文件名**：`fig-4-4-iconpicker.png`

### 图11　UI-02 添加成功后首页新卡片

- **操作**：上一步选一个 3D 图标（如"耳机"），填名称"测试-耳机"、价格，保存。
- **拍什么**：返回首页后，列表里出现这张带 3D 图标的新卡片。
- **文件名**：`fig-4-4-additem.png`

### 图12　UI-04 删除确认框

- **操作**：进任意物品详情页 → 点删除（或列表里长按/滑动删除）→ 出现确认弹框时**先别确认**。
- **拍什么**：" 确认删除？"的对话框（含"取消/删除"按钮）。
- **文件名**：`fig-4-4-delete-confirm.png`

### 图13　UI-04 删除后列表

- **操作**：点确认删除。
- **拍什么**：返回列表，刚才那件物品已消失。
- **文件名**：`fig-4-4-delete-after.png`

### 图14　UI-06 头像修改

- **操作**：设置页点头像 → 选一张本地图片 → 上传。
- **拍什么**：头像更新后的设置页（可把"修改前默认头像"和"修改后"拼成一张左右对比图）。
- **文件名**：`fig-4-4-avatar.png`

---

## 三·五、用开发者工具拍"技术证据"图（更专业，强烈推荐）

界面图证明"功能能用"，**DevTools 图证明"底层怎么跑"**——请求带没带令牌、返回的 `{code,message,data}` 长什么样、主题色是不是真存进了浏览器。这类图最能体现专业度，建议给 4.2 / 4.3 / UI-01 各补一张，与 curl / 界面图互为印证。

### 通用设置（先做一次）

1. `F12` 打开开发者工具；
2. 切到 **Network（网络）** 面板；
3. 勾选顶部 **Preserve log（保留日志）** 和 **Disable cache（禁用缓存）**；
4. 左侧过滤器选 **Fetch/XHR**，只看接口请求，排除图片/脚本干扰；
5. 操作页面时请求会实时出现在列表里，点中某条 → 右侧看 **Headers / Payload / Response** 三个子页。

> 截图时把"请求名 + 状态码 + 右侧 Headers/Response"一起框进去，证据才完整。

### DT-1　登录请求与返回的 JWT（配 4.2 TC-01）

- **操作**：在登录页正常登录一次，Network 里点中 `Login` 这条请求。
- **拍什么（两张或一张拼）**：
  - **Payload** 子页：请求体 `{studentId, password}`（密码是 AES 密文，正好体现"前端加密后再传"）；
  - **Response** 子页：返回 `{code:200, data:{token:"xxx.yyy.zzz", student:{...}}}`，能看到三段式 JWT。
- **文件名**：`fig-dt-1-login.png`

### DT-2　受保护接口携带令牌 + 数据隔离（配 4.2 TC-02）

- **操作**：登录后停在首页，Network 里点中 `items` 请求。
- **拍什么**：
  - **Headers → Request Headers**：能看到 `Authorization: Bearer eyJ...`（证明每个业务请求都带令牌）；
  - **Response**：返回的物品数组——都是当前用户的（体现按 `user_id` 隔离）。
- **文件名**：`fig-dt-2-items-auth.png`

### DT-3　未授权返回 401（配 4.3 TC-05 / TC-06）

- **操作**：① 退出登录后直接在地址栏访问需要鉴权的接口，或 ② 用 DevTools Console 发一条不带令牌的请求：
  ```js
  fetch('/api/items').then(r => r.json()).then(console.log)
  ```
- **拍什么**：Network 里该请求 **Status 列显示红色 401**，Response 子页是 `{code:401, message:"未登录或令牌已过期"}`。
- **文件名**：`fig-dt-3-401.png`

### DT-4　主题色持久化的"实锤"（配 4.4 UI-01）⭐

> 这张最值得拍——直接证明颜色存进了浏览器，比"刷新还在"更硬核。

- **操作**：换一个主题色后，切到 DevTools 的 **Application（应用）** 面板 → 左侧 **Storage → Local Storage → `http://localhost:3000`**。
- **拍什么**：键值列表里的 **`oneday_accent` = `#xxxxxx`**（值正好是你刚选的颜色十六进制）。可与首页换色后的界面拼一张。
- **文件名**：`fig-dt-4-localstorage.png`

### DT-5　添加物品的请求体（配 4.4 UI-02）

- **操作**：新建物品并选一个 3D 图标后点保存，Network 里点中 `POST /api/items`。
- **拍什么**：**Payload** 子页的请求体，能看到 `name`、`purchase_price`、以及 **`icon_kind:"3d"`、`icon_value:"/public/icons/3d/...png"`**——直观体现图标选择落库的数据。
- **文件名**：`fig-dt-5-additem-payload.png`

### DT-6　心愿 → 资产请求（配 4.2 TC-03，可选）

- **操作**：点心愿物品的"移入资产"，Network 里点中 `acquire` 请求。
- **拍什么**：Response 返回更新后的物品 `{is_wish:false, status:"using", ...}`，证明状态翻转成功。
- **文件名**：`fig-dt-6-acquire.png`

### DT-7　资产闭环"数值三连图"（配 4.2 TC-03）⭐ 推荐替代图3/图4

> 图3/图4 用界面"看数字"不够严谨，改用 DevTools 拍 `GET /api/dashboard` 的 Response 里 `total_assets` 的变化，是整份测试里**最硬的一张证据**。三个时点各刷新一次首页（会触发一次 dashboard 请求），各拍一张 Response：

- **操作 + 拍什么**：
  1. **基线**：登录后停在首页，Network 点中 `dashboard`，Response 里记下 `total_assets`（如 `141749.99`）→ `fig-dt-7a-baseline.png`；
  2. **加 ¥2000 心愿后**：新建一件 ¥2000 心愿物品，回首页刷新，再拍 `dashboard` 的 Response → `total_assets` **仍是 141749.99**（不变）→ `fig-dt-7b-after-wish.png`；
  3. **移入资产后**：把该心愿"移入资产"，回首页刷新，再拍 `dashboard` → `total_assets` **变成 143749.99**（+2000）→ `fig-dt-7c-after-acquire.png`。
- **文件名**：`fig-dt-7a-baseline.png` / `fig-dt-7b-after-wish.png` / `fig-dt-7c-after-acquire.png`

> 这三张数值图与报告 4.2 TC-03 的资产变化表一一对应（141749.99 → 141749.99 → 143749.99），文字 + 截图互证，专业度最高。

---

## 四、拍完怎么贴进报告

### 方式 A：贴进 Word（docx，推荐）

在 Word 里打开《实践课作业.docx》，找到每个「（此处插入运行截图…）」占位，把对应图片插进去，并加图注：

```
图 4-1  登录页
图 4-2  首页仪表盘与物品列表
……
```

### 方式 B：贴进 md（再导出 docx）

把 md 里的占位行替换成图片引用，例如：

```markdown
![图4-1 登录页](images/fig-4-2-login.png)
![图4-2 首页仪表盘与物品列表](images/fig-4-2-home.png)
```

图片放进报告同目录的 `images/` 文件夹，再用 pandoc 重导 docx，图片会一并带入。

---

## 五、速查表

| 编号 | 报告位置 | 一句话拍什么 | 文件名 |
|---|---|---|---|
| 图1 | 4.2 ① | 登录页表单 | fig-4-2-login.png |
| 图2 | 4.2 ② | 首页仪表盘 + 物品列表 | fig-4-2-home.png |
| 图3 | 4.2 ③ | 心愿物品移入前（带"移入资产"按钮） | fig-4-2-wish-before.png |
| 图4 | 4.2 ③ | 移入后总资产 +2000 | fig-4-2-wish-after.png |
| 图5 | 4.3 ① | 注册表单红字校验 | fig-4-3-register-error.png |
| 图6 | 4.3 ② | 错误密码"学号或密码错误"提示 | fig-4-3-login-401.png |
| 图7 | 4.4 UI-01 | 换色前 | fig-4-4-theme-before.png |
| 图8 | 4.4 UI-01 | 换色后立即变色 | fig-4-4-theme-after.png |
| 图9 | 4.4 UI-01 | 刷新后仍是新色 | fig-4-4-theme-reload.png |
| 图10 | 4.4 UI-02 | 图标选择器 3D Tab 网格 | fig-4-4-iconpicker.png |
| 图11 | 4.4 UI-02 | 首页新增 3D 图标卡片 | fig-4-4-additem.png |
| 图12 | 4.4 UI-04 | 删除确认框 | fig-4-4-delete-confirm.png |
| 图13 | 4.4 UI-04 | 删除后列表 | fig-4-4-delete-after.png |
| 图14 | 4.4 UI-06 | 头像修改前后 | fig-4-4-avatar.png |

**DevTools 技术证据图（推荐补拍）：**

| 编号 | 配套用例 | 一句话拍什么 | 文件名 |
|---|---|---|---|
| DT-1 | 4.2 TC-01 | Network：登录请求体(AES密文) + 返回三段式 JWT | fig-dt-1-login.png |
| DT-2 | 4.2 TC-02 | Network：items 请求头 `Authorization: Bearer` + 返回本人物品 | fig-dt-2-items-auth.png |
| DT-3 | 4.3 TC-05/06 | Network：401 红色状态 + `{code:401}` 响应体 | fig-dt-3-401.png |
| DT-4 ⭐ | 4.4 UI-01 | Application → Local Storage 里 `oneday_accent=#xxxxxx` | fig-dt-4-localstorage.png |
| DT-5 | 4.4 UI-02 | Network：POST items 请求体含 `icon_kind/icon_value` | fig-dt-5-additem-payload.png |
| DT-6 | 4.2 TC-03 | Network：acquire 返回 `is_wish:false, status:using` | fig-dt-6-acquire.png |
| DT-7 ⭐ | 4.2 TC-03 | dashboard 的 `total_assets` 三连：基线/加心愿不变/移入+2000 | fig-dt-7a~7c.png |
